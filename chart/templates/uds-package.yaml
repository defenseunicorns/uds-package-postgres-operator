{{- if .Values.postgresql.enabled }}
apiVersion: uds.dev/v1alpha1
kind: Package
metadata:
  name: postgres
  namespace: {{ .Release.Namespace }}
spec:
  network:
    allow:
      - direction: Ingress
        remoteGenerated: IntraNamespace
      - direction: Egress
        remoteGenerated: IntraNamespace
      # Template ingress From out to allow for overrides
      # - direction: Ingress
      #   selector:
      #     cluster-name: pg-cluster
      #   remoteGenerated: Anywhere
      {{- if .Values.postgresql.ingress }}
      - direction: Ingress
        selector:
          cluster-name: pg-cluster
      {{ .Values.postgresql.ingress | toYaml | indent 4 }}
      {{- end }}
      - direction: Egress
        selector:
          cluster-name: pg-cluster  
        remoteGenerated: KubeAPI
{{- end }}